@model IEnumerable<ExerciseWebApp.Models.company>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.is_Active)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.company_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.create_date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.number_of_employees)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.is_Active)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.company_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.create_date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.number_of_employees)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.company_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.company_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.company_id })
        </td>
    </tr>
}

</table>
